//Create a method called sum_these_numbers which takes two integers as an argument and prints their sum to the screen.
function sum_these_numbers(a, b) {
	var sum = a + b;
	console.log(sum);
}

sum_these_numbers(2, 4)